

/**

  for (int ubicacion = 0; ubicacion <= numeros_usuario.length(); ++ubicacion) {
    if (static_cast<int>numeros_usuario[ubicacion] == numero_posicion) {
      return ubicacion;
    } else {
      ubicacion++;
    }
  }

























00];
  for (int indice = 0; indice <= 100; ++indice) {
    std::cin >> numeros_usuario[indice];
  }
  PosicionLetraUsuario(numeros_usuario, numero_posicion);
  return 0;
}

/**

  for (int ubicacion = 0; ubicacion <= numeros_usuario.length(); ++ubicacion) {
    if (static_cast<int>numeros_usuario[ubicacion] == numero_posicion) {
      return ubicacion;
    } else {
      ubicacion++;
    }
  }































int PosicionLetraUsuario(int numero_usuario, int numero_posicion, int& contador) {
  contador++;
  if (numero_usuario == numero_posicion) {
int PosicionLetraUsuario(int numero_usuario, int numero_posicion, int& contador) {
  contador++;
  if (numero_usuario == numero_posicion) {
    return contador;
  }
  return 0;
}

int main() {
  int numero_posicion, numero_usuario, primer_par, contador{0};
  while (std::cin >> numero_usuario) {
    int posicion = PosicionLetraUsuario(numero_usuario, numero_posicion, contador); 
    if (posicion != 0) {
    std::cout << contador << std::endl;
    break;
    }
  } 

  return 0;
}*/